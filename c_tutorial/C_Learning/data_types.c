#include <stdio.h>

int main(int argc, char *argv[])
{
	
	int age = 40; // int -> whole numbers
	double gpa = 3.7; // 40.0
	char grade = 'A'; // single quotes -> single character
	char phrase[] = "Jesen "; // double quotes -> a string(text)

    return 0;
}

